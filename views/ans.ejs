<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Flames Answer</title>

<style>
    *{
        box-sizing: border-box;
        font-family: Arial, Helvetica, sans-serif;
    }

    body{
        margin: 0;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(135deg, #ffb7ff, #b3e5ff);
        color: #222;
        transition: background 0.5s, color 0.5s;
    }

    body.dark {
        background: #121212;
        color: #f1f1f1;
    }

    .card{
        background: white;
        padding: 35px 45px;
        border-radius: 18px;
        box-shadow: 0 10px 25px rgba(0,0,0,0.15);
        text-align: center;
        max-width: 420px;
        transition: background 0.5s, color 0.5s;
    }

    body.dark .card {
        background: #1e1e1e;
        color: #f1f1f1;
    }

    .result{
        font-size: 20px;
        font-weight: bold;
        margin-top: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 16px;
        flex-wrap: wrap;
    }

    .img{
        width: 250px;
        height: 250px;
        object-fit: cover;
        border-radius: 15px;
        display: block;
        margin: 20px auto;
        box-shadow: 0px 0px 10px gray;
    }

    button {
        border-radius: 10px;
        border: none;
        background: #00bcd4;
        color: white;
        font-weight: bold;
        padding: 8px 12px;
        cursor: pointer;
        margin: 10px 5px 0 5px;
        transition: transform 0.2s;
    }

    button:hover {
        transform: scale(1.05);
    }

    p.funny {
        font-size: 14px;
        margin: 5px 0;
    }
</style>
</head>
<body>

<div class="card">

    <h3>🔥 This is only a game — not reality 😄</h3>

    <!-- Dark mode & Music buttons -->
    <div>
        <button id="darkModeToggle">🌙 Dark Mode</button>
        <button onclick="toggleMusic()">🎵 Music On/Off</button>
    </div>

    <!-- Background Music -->
    <audio id="bgMusic" src="/music/indilla.mpeg" autoplay loop></audio>

    <div class="result">
        <div>
            <%= realname %> and <%= ActalFrdname %> become 👉 <span style="color:rgba(0, 128, 0, 0.53)"><%= relationship %></span>
        </div>

        <% if (relationship === 'Friends') { %>
            <img src="/images/friends.jpg" class="img">
            <p class="funny">Friends forever… until someone eats the last pizza slice 🍕😂</p>
        <% } %>

        <% if (relationship === 'Lovers') { %>
            <img src="/images/love.jpg" class="img">
            <p class="funny">💖 Lovers detected! Now stop checking FLAMES and start talking 😏</p>
        <% } %>

        <% if (relationship === 'Affection') { %>
            <img src="/images/affection.jpg" class="img">
            <p class="funny">Lots of affection 😊 but Wi-Fi password still secret!</p>
        <% } %>

        <% if (relationship === 'Marriage') { %>
            <img src="/images/marriage.jpg" class="img">
            <p class="funny">Your future kids are already laughing at this result 😆</p>
        <% } %>

        <% if (relationship === 'Enemies') { %>
            <img src="/images/enemies.jpg" class="img">
            <p class="funny">Oops 😮 You both are enemies… but pizza fixes everything 🍕</p>
        <% } %>

        <% if (relationship === 'Siblings') { %>
            <img src="/images/siblings.jpg" class="img">
            <p class="funny">Siblings detected: free insults for lifetime 😆</p>
        <% } %>
    </div>

    <p class="funny">This is just a game. Don’t break friendships, hearts, or phones because of FLAMES 😅</p>

</div>




<script>
const music = document.getElementById("bgMusic");
function toggleMusic(){
    if(music.paused) music.play();
    else music.pause();
}

const toggle = document.getElementById("darkModeToggle");
toggle.addEventListener("click", () => {
    document.body.classList.toggle("dark");
    toggle.textContent = document.body.classList.contains("dark") ? "☀️ Light Mode" : "🌙 Dark Mode";
});
</script>


</body>
</html>
